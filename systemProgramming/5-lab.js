'use strict';

//For a:= 1 to n begin for b:=1 to n do begin a:=m; end; end;
/*
for(const tokens in hash) {
    for(let i = 0; i < hash[tokens].length; i++) {
      const symbol = hash[tokens][i];
      const length = symbol.length;
      console.log(length)
      const index = str.indexOf(symbol);
      //str = str.slice(index, 1);
      str = str.replace(symbol, '.' * length);
      result.push({ symbol, index });
      //console.log(str)
    }
  }
*/